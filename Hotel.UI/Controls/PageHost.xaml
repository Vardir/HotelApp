<UserControl x:Class="Hotel.UI.Controls.PageHost"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Hotel.UI.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource BackgroundSemiLightBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <!--Content-->
        <Grid Background="{StaticResource BackgroundDarkBrush}"
              Margin="2" Grid.Row="1">

            <Frame x:Name="PageFrame" NavigationUIVisibility="Hidden"
                   Grid.Row="2" Content="{Binding}"/>
        </Grid>
    </Grid>
</UserControl>
